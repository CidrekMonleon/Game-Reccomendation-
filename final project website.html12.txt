<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>GameSphere | Game Recommendation</title>
<style>
  body {
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #1e1e2f, #3b3b98);
    color: white;
    margin: 0;
    padding: 0;
  }
  header {
    text-align: center;
    padding: 15px;
    background: rgba(0, 0, 0, 0.3);
  }
  h1 { color: #ffcc00; margin: 0; }
  p { margin: 5px 0; }
  .container {
    max-width: 650px;
    margin: 40px auto;
    background: rgba(255,255,255,0.1);
    border-radius: 8px;
    padding: 20px;
  }
  input, button {
    width: 100%;
    padding: 10px;
    margin: 8px 0;
    border-radius: 4px;
    border: none;
  }
  input {
    background: #fff;
    color: #333;
  }
  button {
    background: #ffcc00;
    color: #000;
    font-weight: bold;
    cursor: pointer;
  }
  button:hover {
    background: #ff9900;
    color: white;
  }
  .hidden { display: none; }
  .game-card {
    background: rgba(255,255,255,0.15);
    border-radius: 8px;
    padding: 15px;
    margin: 10px 0;
  }
  .search-bar {
    display: flex;
    gap: 5px;
    margin: 15px 0;
  }
  .search-bar input {
    flex: 1;
  }
  footer {
    text-align: center;
    padding: 15px;
    font-size: 13px;
    opacity: 0.7;
  }
</style>
</head>
<body>

<header>
  <h1>🎮 GameSphere</h1>
  <p>Your Game Recommendation Hub</p>
</header>

<div class="container" id="loginPage">
  <h2>Login</h2>
  <input type="text" id="username" placeholder="Username">
  <input type="password" id="password" placeholder="Password">
  <button onclick="login()">Login</button>
  <p style="font-size: 14px; text-align:center;">Don't have an account? <b>Signup feature coming soon!</b></p>
</div>

<div class="container hidden" id="homePage">
  <h2>Welcome, <span id="userDisplay"></span> 👋</h2>

  <div class="search-bar">
    <input type="text" id="searchInput" placeholder="Search by name or genre...">
    <button onclick="searchGames()">Search</button>
  </div>

  <h3>Recommended for You</h3>
  <div id="gameList"></div>

  <hr style="margin:20px 0; border-color: rgba(255,255,255,0.2);">

  <p><i>Feature in progress:</i> Add your favorite games and rate them ⭐ (coming soon)</p>
  <button onclick="logout()">Logout</button>
</div>

<footer>
  <p>GameSphere Project | © 2025 | <i>v0.8 (in development)</i></p>
</footer>

<script>
  const games = [
    { name: "Elden Ring", genre: "RPG", desc: "An open-world fantasy RPG full of mystery and challenge." },
    { name: "Valorant", genre: "Shooter", desc: "Fast-paced tactical shooter with unique agent abilities." },
    { name: "Stardew Valley", genre: "Simulation", desc: "A relaxing life and farm simulation game." },
    { name: "Hollow Knight", genre: "Adventure", desc: "A deep, atmospheric action-platformer." },
    { name: "Celeste", genre: "Platformer", desc: "Climb a mountain and overcome your fears." },
    { name: "Cyberpunk 2077", genre: "Action RPG", desc: "A story-driven open-world set in the future." },
    { name: "Overwatch 2", genre: "Shooter", desc: "Team-based hero shooter with colorful characters." }
  ];

  const loginPage = document.getElementById("loginPage");
  const homePage = document.getElementById("homePage");
  const userDisplay = document.getElementById("userDisplay");
  const gameList = document.getElementById("gameList");

  function login() {
    const username = document.getElementById("username").value.trim();
    const password = document.getElementById("password").value.trim();

    if (!username || !password) {
      alert("Please enter both username and password!");
      return;
    }

    localStorage.setItem("currentUser", username);
    showHome(username);
  }

  function logout() {
    localStorage.removeItem("currentUser");
    homePage.classList.add("hidden");
    loginPage.classList.remove("hidden");
  }

  function showHome(user) {
    loginPage.classList.add("hidden");
    homePage.classList.remove("hidden");
    userDisplay.textContent = user;

    displayGames(games);
  }

  function displayGames(gameArray) {
    gameList.innerHTML = "";
    if (gameArray.length === 0) {
      gameList.innerHTML = "<p>No games found. Try another search!</p>";
      return;
    }

    gameArray.forEach(g => {
      const card = document.createElement("div");
      card.className = "game-card";
      card.innerHTML = `
        <h3>${g.name}</h3>
        <p><b>Genre:</b> ${g.genre}</p>
        <p>${g.desc}</p>
        <!-- TODO: Add rating and favorite buttons -->
      `;
      gameList.appendChild(card);
    });
  }

  function searchGames() {
    const query = document.getElementById("searchInput").value.toLowerCase();
    const filtered = games.filter(g => g.name.toLowerCase().includes(query) || g.genre.toLowerCase().includes(query));
    displayGames(filtered);
  }

  window.onload = function() {
    const savedUser = localStorage.getItem("currentUser");
    if (savedUser) showHome(savedUser);
  };
</script>
</body>
</html>
